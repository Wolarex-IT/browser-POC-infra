# Top-level dependencies. Compile to requirements.txt with:
#   pip-compile requirements.in

# Web framework + server for the API.
fastapi==0.136.3
uvicorn==0.48.0
gunicorn==26.0.0
# Request/response models and validation.
pydantic==2.13.4

# ORM (async in the app, sync in the worker) + both Postgres drivers.
SQLAlchemy[asyncio]==2.0.50
asyncpg==0.31.0
psycopg[binary]==3.3.4

# Task queue (RabbitMQ broker; no result backend - the jobs table is the source of truth).
celery==5.6.3

# Drive CloakBrowser over CDP, and spawn its container via the Docker SDK (DooD).
playwright==1.60.0
docker==7.1.0
